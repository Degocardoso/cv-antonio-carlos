[build]
  publish = "."
  functions = "netlify/functions"

# Garante que as funções respondem com CORS correto
[[headers]]
  for = "/.netlify/functions/*"
  [headers.values]
    Access-Control-Allow-Origin = "*"
